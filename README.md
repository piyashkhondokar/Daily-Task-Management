🎯 প্রজেক্টের নাম:

Daily Task Management (দৈনন্দিন কাজ ব্যবস্থাপনা)

💬 ভূমিকা:

শ্রদ্ধেয় স্যার,
আসসালামু আলাইকুম।

আমি আমার তৈরি করা অ্যান্ড্রয়েড প্রজেক্ট "Daily Task Management" আপনার সামনে উপস্থাপন করছি।
এই অ্যাপটি তৈরি করার মূল উদ্দেশ্য হলো — আমাদের দৈনন্দিন জীবনের কাজগুলোকে একটি ডিজিটাল প্ল্যাটফর্মে সহজভাবে গুছিয়ে রাখা এবং ব্যবস্থাপনা করা।

📱 অ্যাপের কার্যকারিতা (What the App Does):

এই অ্যাপটি ব্যবহার করে একজন ব্যবহারকারী তার প্রতিদিনের কাজগুলো খুব সহজে তালিকাভুক্ত করতে পারবেন।
এর প্রধান সুবিধাগুলো হলো —

1️⃣ নতুন কাজ যোগ করা:
ব্যবহারকারী একটি শিরোনাম (Title), বিস্তারিত বিবরণ (Description) এবং নির্দিষ্ট তারিখ (Due Date) দিয়ে নতুন কাজ যোগ করতে পারবেন।

2️⃣ কাজের তালিকা দেখা:
হোম স্ক্রিনে সব কাজ একটি সুন্দর ও পরিচ্ছন্ন তালিকায় দেখা যাবে, যাতে ব্যবহারকারী তার কাজের অবস্থা এক নজরে বুঝতে পারেন।

3️⃣ কাজ সম্পাদনা (Edit) করা:
যদি কোনো কাজের তথ্য পরিবর্তন করার প্রয়োজন হয় (যেমন তারিখ বদলানো বা বিবরণে কিছু যোগ করা), সেটি সহজেই করা যাবে।

4️⃣ কাজের অবস্থা (Status) পরিবর্তন:
প্রতিটি কাজ শেষ হয়ে গেলে সেটিকে “Completed” (সম্পন্ন) হিসেবে চিহ্নিত করার সুবিধা রয়েছে।

⚙️ ডেভেলপমেন্ট প্রক্রিয়া (Development Process):

1️⃣ প্রোগ্রামিং ভাষা:
পুরো অ্যাপটি আমি Kotlin প্রোগ্রামিং ভাষা ব্যবহার করে তৈরি করেছি।
Kotlin আধুনিক, নিরাপদ এবং সংক্ষিপ্ত কোড লেখায় সাহায্য করে।

2️⃣ ইউজার ইন্টারফেস (UI) ডিজাইন:
অ্যাপটির ডিজাইন করার জন্য আমি XML (Extensible Markup Language) ব্যবহার করেছি।
এছাড়াও Material Design-এর বিভিন্ন কম্পোনেন্ট ব্যবহার করেছি, যেমন —

CardView: প্রতিটি কাজকে সুন্দরভাবে আলাদা কার্ড আকারে দেখানোর জন্য।

RecyclerView: একাধিক কাজ স্ক্রোলযোগ্য তালিকায় দেখানোর জন্য।

FloatingActionButton (FAB): ‘+’ বাটন ক্লিক করলে নতুন কাজ যোগ করার পেজে নেওয়ার জন্য।

Toolbar: অ্যাপের উপরে শিরোনাম ও নেভিগেশন কন্ট্রোল দেখানোর জন্য।

DatePicker: সহজে ক্যালেন্ডার থেকে তারিখ বেছে নেওয়ার জন্য।

3️⃣ ডেটা সংরক্ষণ (Data Storage):
অ্যাপের সবচেয়ে গুরুত্বপূর্ণ অংশ হলো ডেটা সংরক্ষণ করা।
এর জন্য আমি Room Persistence Library ব্যবহার করেছি।
Room হলো একটি আধুনিক ডেটাবেস লাইব্রেরি যা SQLite-এর উপর ভিত্তি করে কাজ করে।
এতে আমি Task নামে একটি টেবিল তৈরি করেছি, যেখানে ব্যবহারকারীর কাজের তথ্য সংরক্ষিত থাকে।

4️⃣ আর্কিটেকচার (Architecture):
অ্যাপটি আমি MVVM (Model–View–ViewModel) আর্কিটেকচার অনুসরণ করে তৈরি করেছি।

Model: Task.kt ক্লাসটি ডেটা স্ট্রাকচার হিসেবে কাজ করে।

View: MainActivity, AddTaskActivity এবং XML ফাইলগুলো UI পরিচালনা করে।

ViewModel: যদিও সম্পূর্ণভাবে প্রয়োগ করিনি, ভবিষ্যতে UI ও ডেটা লজিককে আলাদা রাখতে এটি ব্যবহার করার পরিকল্পনা রয়েছে।

5️⃣ অ্যাক্টিভিটির মধ্যে ডেটা পাসিং (Data Passing Between Activities):
এক স্ক্রিন থেকে অন্য স্ক্রিনে ডেটা পাঠানোর জন্য আমি Intent ব্যবহার করেছি।
ডেটা পাসের জন্য ব্যবহৃত key-গুলোকে companion object-এর মধ্যে const val হিসেবে ঘোষণা করেছি, যাতে কোড সহজ ও ত্রুটিমুক্ত থাকে।

🧠 আমার অভিজ্ঞতা ও ভবিষ্যৎ পরিকল্পনা:

এই প্রজেক্টটি করতে গিয়ে আমি অ্যান্ড্রয়েড অ্যাপ ডেভেলপমেন্টের অনেক গুরুত্বপূর্ণ বিষয় শিখেছি, যেমন —

UI ডিজাইন

ডেটাবেস ম্যানেজমেন্ট

একাধিক Activity-র মধ্যে ডেটা আদান–প্রদান

ভবিষ্যৎ পরিকল্পনা:
ভবিষ্যতে আমি অ্যাপটিতে আরও কিছু ফিচার যোগ করার পরিকল্পনা করছি —

কাজের জন্য রিমাইন্ডার বা নোটিফিকেশন সেট করা।

কাজগুলোকে বিভিন্ন ক্যাটাগরিতে (যেমন অফিস, বাসা ইত্যাদি) ভাগ করা।

একটি সার্চ বার যোগ করা, যাতে ব্যবহারকারী সহজে কাজগুলো খুঁজে পেতে পারেন।

🙏 সমাপ্তি:

আমার এই প্রজেক্টটি আপনার গাইডলাইন ও অনুপ্রেরণার ফল।
আশা করি আমার এই প্রচেষ্টা আপনার ভালো লাগবে।

ধন্যবাদান্তে,
পিয়াস খন্দকার
ব্যাচ: ২৫০১

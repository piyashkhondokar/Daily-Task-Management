📘 Project Report: Daily Task Management (দৈনন্দিন কাজ ব্যবস্থাপনা)
🏫 Department of Computer Science
Project Title: Task Nest – Android Task Manager App

Student Name: [piyas khandakar]
ব্যাচ: ২৫০১

🧩 1. Abstract

এই প্রজেক্টে আমি Kotlin ও Room Database ব্যবহার করে একটি Android Task Manager অ্যাপ তৈরি করেছি, যার নাম Task Nest।
অ্যাপটি ইউজারদের দৈনন্দিন কাজগুলো সংগঠিতভাবে রাখতে সাহায্য করে।
ইউজাররা টাস্ক যোগ করতে, আপডেট করতে, ডিলিট করতে এবং সার্চের মাধ্যমে খুঁজে পেতে পারে।
লোকাল ডাটাবেস ব্যবহারের ফলে অ্যাপটি অফলাইনেও কাজ করে।

🧱 2. Introduction

Task Nest প্রজেক্টের মূল উদ্দেশ্য ছিল Android Development-এর বেসিক ও ডাটাবেস ইন্টিগ্রেশন শেখা।
এই প্রজেক্টের মাধ্যমে আমি Kotlin প্রোগ্রামিং, Room Database, RecyclerView এবং Material Design নিয়ে হাতে-কলমে অভিজ্ঞতা অর্জন করেছি।

⚙️ 3. Technologies Used
Section	Technology
Language	Kotlin
Database	Room Database
UI Components	RecyclerView, CardView, FloatingActionButton
Design	Material Design
Utility	View Binding, DatePicker, SearchView, AlertDialog
🧩 4. System Overview
Main Features:

নতুন টাস্ক যোগ করা

টাস্ক এডিট ও আপডেট করা

কনফার্মেশন সহ ডিলিট করা

টাস্ক সম্পূর্ণ হিসেবে মার্ক করা

রিয়েল-টাইম সার্চ ফিচার

তারিখ নির্বাচন করার সুবিধা

💻 5. Implementation
Database Layer

Room Database ব্যবহার করে লোকাল ডাটা সংরক্ষণ করা হয়েছে।

@Entity(tableName = "tasks")
data class Task(
    @PrimaryKey(autoGenerate = true)
    val id: Int = 0,
    val title: String,
    val description: String,
    val dueDate: String,
    val isCompleted: Boolean = false
)


DAO interface ব্যবহার করে CRUD অপারেশন করা হয়েছে।

UI Layer

RecyclerView এর মাধ্যমে সব টাস্ক লিস্ট আকারে দেখানো হয়েছে। 


Add/Edit screen থেকে টাস্ক যোগ বা পরিবর্তন করা যায়।

🧠 6. Problems & Solutions
সমস্যা	সমাধান
Room dependency কাজ করছিল না	build.gradle এ kapt যোগ করে ঠিক করা
RecyclerView আপডেট হচ্ছিল না	notifyDataSetChanged() ব্যবহার
Date format mismatch	SimpleDateFormat ও Locale ঠিক করা
Search bar reset সমস্যা	clear logic manually implement করা
🎨 7. User Interface (UI/UX)

Empty state এ “No Tasks Available” দেখানো

Toast ও Dialog দিয়ে user feedback

Material Design ফলো করে modern layout

Orange theme ও rounded corners ব্যবহার

📚 8. Learning Outcomes

Room Database setup ও ব্যবহার ডেটা সংরক্ষণ (Data Storage):
অ্যাপের সবচেয়ে গুরুত্বপূর্ণ অংশ হলো ডেটা সংরক্ষণ করা।
এর জন্য আমি Room Persistence Library ব্যবহার করেছি।
Room হলো একটি আধুনিক ডেটাবেস লাইব্রেরি যা SQLite-এর উপর ভিত্তি করে কাজ করে।
এতে আমি Task নামে একটি টেবিল তৈরি করেছি, যেখানে ব্যবহারকারীর কাজের তথ্য সংরক্ষিত থাকে।


RecyclerView adapter তৈরি

View Binding ও Type Safety

Activity lifecycle বোঝা

Clean code ও proper documentation

Debugging ও Logcat ব্যবহার

🔮 9. Future Improvements

Task category ও priority level

Reminder notification

Firebase দিয়ে cloud sync

Dark mode ও statistics graph

Swipe gesture দিয়ে delete

🏁 10. Conclusion

এই প্রজেক্টটি করার মাধ্যমে আমি Android development-এর practically দিকগুলো শিখতে পেরেছি।
Room Database, RecyclerView, এবং Kotlin এর বিভিন্ন feature বাস্তবে ব্যবহার করে বুঝেছি আসলে কীভাবে একটি অ্যাপ কাজ করে।
সবচেয়ে বড় শিক্ষা হলো — theory পড়ার চেয়ে হাতে-কলমে কাজ করলেই শেখা বেশি হয়
